export enum Variant {
  Default = 'default',
  Icon = 'icon',
  Card = 'card'
}

export enum PaginationHeaders {
  pageNumber = 'X-Page-Number',
  pageSize = 'X-Page-Size',
  pagination = 'x-pagination'
}
